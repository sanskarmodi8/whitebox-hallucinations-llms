# Results storage

Large experiment outputs (full model checkpoints, full dataset dumps, large result archives) are stored externally:
- Google Drive: <link>

Each run-manifest.json in `experiments/<run>/` contains a link + checksum.